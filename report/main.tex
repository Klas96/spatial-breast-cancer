\documentclass[12pt, a4paper]{article}

% Packages
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\newcommand{\maybeincludegraphics}[1]{\includegraphics[width=\linewidth]{#1}}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{parskip}
\usepackage{microtype}
\usepackage{natbib}
\usepackage{lineno}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\definecolor{highlightbg}{RGB}{240,246,255}
\usepackage{longtable}
\usepackage{listings}
\lstset{
    language=bash,
    basicstyle=\small\ttfamily,
    backgroundcolor=\color{gray!7},
    frame=single, framerule=0.4pt, rulecolor=\color{gray!35},
    breaklines=true, breakatwhitespace=true,
    showstringspaces=false, tabsize=4,
    xleftmargin=4pt, xrightmargin=4pt,
    aboveskip=5pt, belowskip=3pt,
}
\lstdefinestyle{python}{
    language=Python,
    keywordstyle=\color{blue!65!black}\bfseries,
    commentstyle=\color{green!45!black}\itshape,
    stringstyle=\color{orange!75!black},
}
% STAR Protocols annotation commands
\newcommand{\critical}[1]{\par\smallskip\noindent%
    {\color{red!65!black}\bfseries CRITICAL:}\ #1\par\smallskip}
\newcommand{\notepar}[1]{\par\smallskip\noindent%
    {\bfseries NOTE:}\ #1\par\smallskip}
\newcommand{\pausept}[1]{\par\smallskip\noindent%
    {\bfseries PAUSE POINT:}\ #1\par\smallskip}
\newcommand{\timing}[1]{%
    \begin{tcolorbox}[colback=orange!7,colframe=orange!55!black,
        fonttitle=\bfseries\small,title=Timing,
        boxrule=0.5pt,left=4pt,right=4pt,top=2pt,bottom=2pt,
        before upper=\small]
    #1
    \end{tcolorbox}\vspace{2pt}}

\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=blue!60!black,
    urlcolor=blue!60!black
}

\titleformat{\section}{\large\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{1em}{}

\onehalfspacing

% ---------------------------------------------------------------
\title{\textbf{Spatial Characterisation of the Breast Cancer\\
Tumour Microenvironment via Spatial Transcriptomics\\
and Graph Neural Networks}\\[0.5em]
\large An End-to-End Computational Analysis Pipeline}

\author{Klas Holmgren}
\date{\today}

\begin{document}

\maketitle

% ---------------------------------------------------------------
% Highlights box (STAR Protocols style)
\begin{tcolorbox}[
    enhanced,
    colback=highlightbg,
    colframe=blue!40!black,
    fonttitle=\bfseries,
    title=Highlights,
    arc=4pt,
    left=6pt, right=6pt, top=4pt, bottom=4pt
]
\begin{itemize}\setlength\itemsep{2pt}
    \item End-to-end Python pipeline for spatial transcriptomics analysis of the tumour microenvironment
    \item Bayesian cell-type deconvolution using Cell2location with the Wu et al.\ 2021 breast cancer atlas
    \item Spatially variable gene detection and neighbourhood co-localisation analysis via Squidpy
    \item Graph convolutional network predicts tissue domains from the spatial gene expression graph
\end{itemize}
\end{tcolorbox}

\vspace{0.8em}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{../report/graphical_abstract.png}
    \captionsetup{skip=4pt}
    \caption*{\small\textit{Graphical abstract: the six-stage pipeline from raw Visium input to GCN tissue domain prediction, validated on two independent breast cancer datasets.}}
\end{figure}

\vspace{0.5em}
\tableofcontents
\newpage

% ---------------------------------------------------------------
\section{Abstract}

Spatial transcriptomics enables simultaneous profiling of gene expression and tissue architecture, but integrating the full analytical workflow---from quality control to deep learning---requires coordinating multiple specialised tools. Here we present a step-by-step computational protocol for characterising the breast cancer tumour microenvironment using 10x Genomics Visium data. The pipeline covers quality control, normalisation, Leiden clustering, Bayesian cell-type deconvolution with Cell2location, spatially variable gene detection using Moran's $I$, cell-type co-localisation analysis, and tissue domain prediction with a graph convolutional network (GCN). We validate the protocol on two independent Visium datasets---one FFPE and one fresh-frozen---demonstrating reproducibility across sample preparation methods. All steps are implemented in Python using a \texttt{uv}-managed environment and can be executed sequentially via a single shell script. The protocol is designed for computational biologists with basic Python experience and requires approximately 4 hours of compute time per dataset with GPU acceleration. Code is available at \url{https://github.com/Klas96/spatial-breast-cancer}.

% ---------------------------------------------------------------
\section{Introduction}

Breast cancer is the most common malignancy in women worldwide, with outcome strongly influenced not only by the intrinsic properties of tumour cells but also by the composition and spatial organisation of the surrounding TME \citep{wu2021}. The TME comprises cancer-associated fibroblasts (CAFs), endothelial cells, tumour-infiltrating lymphocytes (TILs), myeloid cells, and normal epithelial cells, each playing distinct roles in tumour progression and immune evasion.

Single-cell RNA sequencing (scRNA-seq) has transformed our understanding of cellular heterogeneity within tumours; however, it requires tissue dissociation, irrevocably losing spatial context. Spatial transcriptomics methods, pioneered by \citet{stahl2016} and commercialised by 10x Genomics as the Visium platform, overcome this limitation by measuring gene expression at spatially barcoded spots on intact tissue sections, preserving the tissue architecture while capturing near-transcriptome-wide expression profiles.

The integration of spatial transcriptomics with matched scRNA-seq reference atlases allows cell-type proportions to be estimated at each spatial spot—a process termed \emph{deconvolution}. Cell2location \citep{kleshchevnikov2022} is a principled Bayesian method for this task, modelling observed spot expression as a linear combination of cell-type signatures learned from a scRNA-seq reference.

Beyond deconvolution, the spatial structure of the TME can be further interrogated through spatially variable gene (SVG) analysis \citep{moran1950}, neighbourhood enrichment testing \citep{palla2022}, and, more recently, graph-based machine learning. Representing the tissue as a graph—where spots are nodes connected by spatial adjacency—naturally captures local microenvironmental context. Graph convolutional networks (GCNs) \citep{kipf2017} can leverage this relational structure to predict tissue domain labels in a semi-supervised manner.

In this work, we apply this complete analytical framework to a publicly available 10x Visium FFPE dataset from human breast cancer tissue, using the Wu et al. \citeyearpar{wu2021} breast cancer scRNA-seq atlas as a deconvolution reference.

% ---------------------------------------------------------------
\section{Data}

\subsection{Spatial transcriptomics data}

The spatial dataset used in this study is the 10x Genomics Visium FFPE Human Breast Cancer dataset (SpaceRanger v1.3.0), available at \url{https://www.10xgenomics.com/datasets}. The dataset comprises a single tissue section from formalin-fixed paraffin-embedded (FFPE) human breast cancer, profiled on a Visium capture area of $6.5 \times 6.5$\,mm. Each spot has a diameter of 55\,$\mu$m and a centre-to-centre distance of 100\,$\mu$m, yielding a maximum of $\sim$5,000 spots per section. Gene expression was quantified across 18,085 genes.

\subsection{scRNA-seq reference atlas}

Cell-type deconvolution requires a matched single-cell RNA-seq reference. We employed the Wu et al. \citeyearpar{wu2021} breast cancer single-cell atlas (Zenodo accession 4739739), comprising 15,611 cells from six tumours spanning multiple subtypes (ER$^+$, HER2$^+$, TNBC). The atlas contains histological region annotations used as cell-type labels for deconvolution signature estimation.

% ---------------------------------------------------------------
\section{Key Resources Table}

\begingroup
\small
\setlength{\LTpre}{4pt}
\setlength{\LTpost}{4pt}
\begin{longtable}{p{4.5cm} p{4.8cm} p{5.3cm}}
\caption{Key resources used in this protocol.}
\label{tab:resources} \\
\toprule
\textbf{Reagent or resource} & \textbf{Source} & \textbf{Identifier} \\
\midrule
\endfirsthead
\multicolumn{3}{c}{\small\itshape Table~\ref{tab:resources} continued} \\
\toprule
\textbf{Reagent or resource} & \textbf{Source} & \textbf{Identifier} \\
\midrule
\endhead
\midrule \multicolumn{3}{r}{\small\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

%% ── Deposited data ──────────────────────────────────────────────
\multicolumn{3}{l}{\textit{\textbf{Deposited data}}} \\[2pt]

10x Visium FFPE Human Breast Cancer (SpaceRanger v1.3.0) &
10x Genomics &
\url{https://www.10xgenomics.com/datasets} \\[4pt]

10x Visium Fresh-Frozen Human Breast Cancer Block~A Section~1
(SpaceRanger v1.1.0) &
10x Genomics &
\url{https://www.10xgenomics.com/datasets} \\[4pt]

Wu et al.\ 2021 breast cancer scRNA-seq atlas;
100,064 cells, 9 major cell types &
Wu et al., 2021 \citep{wu2021};
CellXGene &
GEO: GSE176078;
DOI: 10.1038/s41588-021-00911-1 \\[4pt]

%% ── Software and algorithms ─────────────────────────────────────
\multicolumn{3}{l}{\textit{\textbf{Software and algorithms}}} \\[2pt]

Python 3.12.3 &
Python Software Foundation &
\url{https://www.python.org} \\[4pt]

uv~0.11.14 — Python package manager &
Astral &
\url{https://github.com/astral-sh/uv} \\[4pt]

Scanpy~1.12.1 — single-cell analysis &
Wolf et al., 2018 \citep{wolf2018} &
PyPI: \texttt{scanpy};
DOI: 10.1186/s13059-017-1382-0 \\[4pt]

Squidpy~1.8.1 — spatial omics analysis &
Palla et al., 2022 \citep{palla2022} &
PyPI: \texttt{squidpy};
DOI: 10.1038/s41592-021-01358-2 \\[4pt]

AnnData~0.12.14 — annotated data matrices &
Virshup et al., 2024 &
PyPI: \texttt{anndata};
DOI: 10.1101/2021.12.16.473007 \\[4pt]

Cell2location~0.1.5 — Bayesian deconvolution &
Kleshchevnikov et al., 2022 \citep{kleshchevnikov2022} &
PyPI: \texttt{cell2location};
DOI: 10.1038/s41587-021-01139-4 \\[4pt]

PyTorch~2.12.0+cu130 — deep learning framework &
Paszke et al., 2019 &
PyPI: \texttt{torch};
\url{https://pytorch.org} \\[4pt]

PyTorch Geometric~2.7.0 — geometric deep learning &
Fey \& Lenssen, 2019 &
PyPI: \texttt{torch-geometric};
DOI: 10.48550/arXiv.1903.02428 \\[4pt]

scikit-learn~1.8.0 — machine learning utilities &
Pedregosa et al., 2011 &
PyPI: \texttt{scikit-learn};
DOI: 10.5555/1953048.2078195 \\[4pt]

igraph~1.0.0 + leidenalg~0.11.0 — Leiden clustering &
Traag et al., 2019 \citep{traag2019} &
PyPI: \texttt{igraph}, \texttt{leidenalg};
DOI: 10.1038/s41598-019-41695-z \\[4pt]

pandas~2.3.3, NumPy~2.4.5 — data manipulation &
McKinney, 2010; Harris et al., 2020 &
PyPI: \texttt{pandas}, \texttt{numpy} \\[4pt]

matplotlib~3.10.9, seaborn~0.13.2 — visualisation &
Hunter, 2007; Waskom, 2021 &
PyPI: \texttt{matplotlib}, \texttt{seaborn} \\[4pt]

Analysis pipeline (this work) &
This paper &
\url{https://github.com/Klas96/spatial-breast-cancer} \\[4pt]

%% ── Other ───────────────────────────────────────────────────────
\multicolumn{3}{l}{\textit{\textbf{Other}}} \\[2pt]

NVIDIA GeForce RTX~4070 GPU (12~GB GDDR6X) &
NVIDIA Corporation &
CUDA~13.2; Driver~595 \\[4pt]

Server: Ubuntu 22.04 LTS, 64~GB RAM,
AMD Ryzen~9 CPU &
N/A &
N/A \\[4pt]

\end{longtable}
\endgroup

% ---------------------------------------------------------------
\section{Methods}

The analysis pipeline consists of six sequential stages implemented in Python using Scanpy \citep{wolf2018} and Squidpy \citep{palla2022}. All code is available at \url{https://github.com/Klas96/spatial-breast-cancer}.

\subsection{Quality control and preprocessing (Stage 1)}

Raw Visium data were loaded via \texttt{sc.read\_visium}. Mitochondrial gene content was computed by flagging genes with names prefixed \texttt{MT-}. Spots failing any of the following thresholds were removed: total UMI count $<1{,}000$, detected genes $<500$, or mitochondrial fraction $>20\%$. After filtering, expression counts were normalised to $10{,}000$ counts per spot (\texttt{normalize\_total}) and log-transformed (\texttt{log1p}). The 3,000 most highly variable genes (HVGs) were identified using the \texttt{seurat\_v3} flavour. Expression values were then z-scored and principal component analysis (PCA) was performed, retaining 50 components.

\subsection{Clustering and dimensionality reduction (Stage 2)}

A $k$-nearest neighbour graph was computed in PCA space ($k=15$, 30 PCs) using Scanpy's \texttt{neighbors} function. Spatial neighbours were additionally computed from tissue coordinates using Squidpy's \texttt{spatial\_neighbors} function with a regular grid layout. Uniform Manifold Approximation and Projection (UMAP) was applied for two-dimensional visualisation. Leiden community detection \citep{traag2019} was run at resolution 0.5 using the igraph backend.

\subsection{Cell-type deconvolution (Stage 3)}

Cell-type proportions at each Visium spot were estimated using Cell2location \citep{kleshchevnikov2022}, a hierarchical Bayesian model that deconvolves spatial expression into contributions from individual cell types.

\paragraph{Reference signature estimation.} The scRNA-seq atlas (15,611 cells, 18,714 genes) was used to train a \texttt{RegressionModel} for 200 epochs on a single NVIDIA GeForce RTX\,4070 GPU. The posterior means of cell-type-specific expression signatures ($\hat{\mu}_{fg}$) were exported as the reference signature matrix.

\paragraph{Spatial model.} The spatial model (\texttt{Cell2location}) was fit to the Visium data for 10,000 epochs, with $N_{\text{cells per spot}} = 8$ (appropriate for breast tissue Visium) and $\alpha_{\text{detection}} = 20$. Cell-type abundance estimates were exported as the posterior mean $w_{sf}$.

\subsection{Spatially variable genes (Stage 4)}

To identify genes whose expression co-varies with spatial position, Moran's $I$ spatial autocorrelation statistic was computed for all genes using Squidpy's \texttt{spatial\_autocorr} function \citep{moran1950}. Moran's $I \in [-1, 1]$, with values close to 1 indicating strong positive spatial autocorrelation. The top 12 SVGs were visualised as spatial expression maps.

\subsection{Neighbourhood enrichment analysis (Stage 5)}

To quantify cell-type co-localisation, each spot was assigned its dominant cell type (the cell type with the highest estimated abundance from Stage 3). Squidpy's \texttt{nhood\_enrichment} function was then applied, which uses a permutation test to compute $z$-scores reflecting whether pairs of cell types are more co-localised than expected by chance.

\subsection{Graph neural network for tissue domain prediction (Stage 6)}

\paragraph{Graph construction.} The tissue was represented as an undirected graph $G = (V, E)$, where each Visium spot $v_i \in V$ is a node with feature vector $\mathbf{x}_i \in \mathbb{R}^{50}$ (the PCA embedding from Stage 1), and edges $E$ are defined by spatial adjacency (from the grid-based spatial neighbour graph). Node labels $y_i$ correspond to the dominant cell type assigned in Stage 5.

\paragraph{Model architecture.} We trained a two-layer graph convolutional network \citep{kipf2017}:
\begin{align}
\mathbf{H}^{(1)} &= \text{ReLU}\!\left(\hat{A}\,\mathbf{X}\,\mathbf{W}^{(0)}\right), \\
\mathbf{H}^{(2)} &= \text{ReLU}\!\left(\hat{A}\,\mathbf{H}^{(1)}\,\mathbf{W}^{(1)}\right), \\
\hat{\mathbf{Y}} &= \mathbf{H}^{(2)}\,\mathbf{W}^{(\text{out})},
\end{align}
where $\hat{A} = \tilde{D}^{-1/2}\tilde{A}\tilde{D}^{-1/2}$ is the symmetrically normalised adjacency matrix with self-loops, and $\mathbf{W}^{(0)}, \mathbf{W}^{(1)} \in \mathbb{R}^{50 \times 64}$, $\mathbf{W}^{(\text{out})} \in \mathbb{R}^{64 \times C}$ are learnable weight matrices ($C$ = number of cell-type classes). Dropout ($p = 0.3$) was applied after each graph convolution layer.

\paragraph{Training.} Spots were randomly split into training (68\%), validation (12\%), and test (20\%) sets. The model was trained for 300 epochs using the Adam optimiser ($\text{lr} = 10^{-3}$, weight decay $= 5 \times 10^{-4}$) minimising cross-entropy loss. Performance was evaluated on the held-out test set using per-class precision, recall, and F1-score.

% ---------------------------------------------------------------
\section{Step-by-Step Method Details}

All steps assume the repository has been cloned and the virtual
environment is active (see Before You Begin below).  Steps are
numbered sequentially; sub-steps are lettered.  The entire pipeline
can also be executed with a single command:
\begin{lstlisting}
./scripts/run_pipeline.sh data/visium results
\end{lstlisting}

% -------------------------------------------------------------------
\subsection*{Before You Begin}

\timing{30~min (one-time setup)}

\begin{enumerate}

\item \textbf{Clone the repository and enter the project directory.}
\begin{lstlisting}
git clone https://github.com/Klas96/spatial-breast-cancer.git
cd spatial-breast-cancer
\end{lstlisting}

\item \textbf{Install \texttt{uv} (if not already installed).}
\begin{lstlisting}
curl -Lsf https://astral.sh/uv/install.sh | sh
source $HOME/.local/bin/env          # add uv to PATH
\end{lstlisting}

\item \textbf{Create the virtual environment and install all dependencies.}
\begin{lstlisting}
uv sync
\end{lstlisting}
This resolves and installs all packages listed in \texttt{pyproject.toml},
including Scanpy, Squidpy, Cell2location, and PyTorch Geometric.
\critical{The project requires Python~$\geq$3.11. Cell2location requires
PyTorch with CUDA support for the GPU-accelerated training steps.
Verify GPU availability with \texttt{nvidia-smi} before proceeding.}

\item \textbf{Download the Visium dataset.}
The 10x Genomics FFPE Human Breast Cancer Visium dataset is freely
available.  Download and place the SpaceRanger output under
\texttt{data/visium/}:
\begin{lstlisting}
mkdir -p data/visium/spatial
BASE="https://cf.10xgenomics.com/samples/spatial-exp/1.3.0/\
Visium_FFPE_Human_Breast_Cancer/Visium_FFPE_Human_Breast_Cancer"
wget -O data/visium/filtered_feature_bc_matrix.h5 \
    "${BASE}_filtered_feature_bc_matrix.h5"
wget -O data/visium/spatial.tar.gz "${BASE}_spatial.tar.gz"
tar -xzf data/visium/spatial.tar.gz -C data/visium/
\end{lstlisting}
The \texttt{spatial/} subdirectory must contain
\texttt{scalefactors\_json.json}, \texttt{tissue\_positions.csv}
(or \texttt{tissue\_positions\_list.csv}), and
\texttt{tissue\_hires\_image.png}.

\item \textbf{Download the Wu et al. 2021 scRNA-seq atlas.}
The atlas is available on CellXGene; download it as an H5AD file
and save it to \texttt{data/wu2021\_scrna\_atlas.h5ad}.
The file must contain \texttt{adata.raw} (raw counts) and
\texttt{adata.obs["celltype\_major"]}.
\notepar{The atlas is $\sim$3.5~GB.  Ensure at least 16~GB of free
disk space before downloading.}

\item \textbf{Verify the directory structure.}
\begin{lstlisting}
ls data/visium/           # filtered_feature_bc_matrix.h5, spatial/
ls data/                  # wu2021_scrna_atlas.h5ad
\end{lstlisting}
\pausept{The environment and data can be kept indefinitely.
Return here before running any analysis stage.}

\end{enumerate}

% -------------------------------------------------------------------
\subsection{Stage 1 — Quality Control and Preprocessing}

\timing{$\sim$5~min}

\begin{enumerate}

\item \textbf{Run the QC and preprocessing script.}
\begin{lstlisting}
PYTHONPATH=src .venv/bin/python src/01_qc_preprocessing.py
\end{lstlisting}

\item \textbf{What the script does (in order):}
\begin{enumerate}
    \item Loads SpaceRanger output via \texttt{sc.read\_visium}.
    \item Computes QC metrics: total UMI counts, genes detected per spot,
          and fraction of reads mapping to mitochondrial genes
          (\texttt{MT-} prefix).
    \item Saves \texttt{results/01\_qc\_distributions.png} — three
          histograms with filter thresholds overlaid.
    \item Removes spots with total counts~$<1{,}000$, detected
          genes~$<500$, or mitochondrial fraction~$>20\%$.
    \item Normalises to $10{,}000$ counts per spot, applies
          \texttt{log1p}, selects the top 3,000 highly variable genes
          (HVGs, \texttt{seurat\_v3} flavour), z-scores, and runs
          PCA retaining 50 components.
    \item Writes \texttt{results/01\_qc\_preprocessed.h5ad}.
\end{enumerate}

\item \textbf{Expected output.}
The console should print the number of spots retained, e.g.:
\begin{lstlisting}
Spots after QC filtering: 2509
Saved: results/01_qc_preprocessed.h5ad
\end{lstlisting}
\critical{If fewer than 500 spots remain after filtering, the QC
thresholds may be too strict for the tissue.  Adjust
\texttt{total\_counts > 1000} or \texttt{pct\_counts\_mt < 20}
inside \texttt{run\_qc()} before continuing.}

\item \textbf{Inspect the QC plot.}
Open \texttt{results/01\_qc\_distributions.png} and verify that the
dashed threshold lines split the distributions sensibly.

\pausept{The \texttt{.h5ad} output can be saved and reloaded at any
later point.  All subsequent stages read from disk, so stages can be
re-run independently.}

\end{enumerate}

% -------------------------------------------------------------------
\subsection{Stage 2 — Clustering and Dimensionality Reduction}

\timing{$\sim$5~min}

\begin{enumerate}

\item \textbf{Run the clustering script.}
\begin{lstlisting}
PYTHONPATH=src .venv/bin/python src/02_clustering.py
\end{lstlisting}

\item \textbf{What the script does:}
\begin{enumerate}
    \item Constructs a $k$-nearest neighbour graph in PCA space
          ($k=15$, 30 PCs).
    \item Builds a spatial neighbour graph from Visium spot
          coordinates (\texttt{coord\_type="grid"}).
    \item Computes a UMAP embedding.
    \item Runs Leiden community detection at resolution~0.5 using the
          igraph backend (\texttt{flavor="igraph",
          directed=False}).
    \item Saves \texttt{results/02\_clusters.png} (UMAP + spatial
          scatter) and \texttt{results/02\_clustered.h5ad}.
\end{enumerate}

\item \textbf{Adjust clustering resolution if needed.}
Open \texttt{src/02\_clustering.py} and change the \texttt{resolution}
argument in \texttt{cluster()}:
\begin{lstlisting}[style=python]
# Finer clusters: increase resolution; coarser: decrease
sc.tl.leiden(adata, resolution=0.5, ...)  # default
\end{lstlisting}
\notepar{Resolution~0.5 yields 6--10 clusters for typical Visium
breast cancer sections.  Higher values produce more clusters.}

\end{enumerate}

% -------------------------------------------------------------------
\subsection{Stage 3 — Cell-Type Deconvolution with Cell2location}

\timing{2--3~h (GPU); 10--15~h (CPU — not recommended)}

\begin{enumerate}

\item \textbf{Run the deconvolution script.}
\begin{lstlisting}
PYTHONPATH=src .venv/bin/python src/03_deconvolution.py
\end{lstlisting}

\item \textbf{What the script does:}
\begin{enumerate}
    \item Reloads raw integer Visium counts from \texttt{data/visium/}
          and subsets to the QC-filtered spots from Stage~1.
    \item Loads the Wu et al.\ atlas, extracts raw counts from
          \texttt{adata.raw}, and maps Ensembl gene IDs to symbols via
          \texttt{adata.raw.var["feature\_name"]}.
    \item Trains the Cell2location \texttt{RegressionModel} on the
          atlas for 200 epochs to estimate cell-type signatures
          ($\hat{\mu}_{fg}$).
    \item Restricts to genes present in both datasets.
    \item Trains the \texttt{Cell2location} spatial model for
          10,000~epochs ($N_{\text{cells}} = 8$,
          $\alpha_{\text{detection}} = 20$).
    \item Exports posterior mean abundances to
          \texttt{adata.obsm["means\_cell\_abundance\_w\_sf"]}.
    \item Saves \texttt{results/03\_cell\_type\_maps.png} and
          \texttt{results/03\_deconvolved.h5ad}.
\end{enumerate}

\critical{Cell2location requires \emph{raw integer counts}.
Do not pass log-normalised or scaled expression; the
\texttt{GammaPoisson} likelihood will produce numerical overflow.
The script reloads raw data from \texttt{data/visium/} for this
reason.}

\item \textbf{Monitor GPU usage (optional).}
In a separate terminal:
\begin{lstlisting}
watch -n 5 nvidia-smi
\end{lstlisting}
GPU memory usage should be $<$4~GB for the spatial model.

\item \textbf{Expected console output.}
\begin{lstlisting}
Shared genes: 15302
Epoch 1/200 [RegressionModel] ...
Epoch 1/10000 [Cell2location] ...
Saved: results/03_cell_type_maps.png
Saved: results/03_deconvolved.h5ad
\end{lstlisting}

\notepar{Shared gene count below 5,000 suggests a gene symbol
mismatch between the Visium data and the atlas.  Verify that
\texttt{adata.raw.var["feature\_name"]} contains HGNC symbols
(e.g.\ \emph{TP53}), not Ensembl IDs (e.g.\ \emph{ENSG00000141510}).}

\pausept{The \texttt{03\_deconvolved.h5ad} file ($\sim$200~MB)
contains all downstream information.  Stages 4--6 can be
re-run from this checkpoint without repeating deconvolution.}

\end{enumerate}

% -------------------------------------------------------------------
\subsection{Stage 4 — Spatially Variable Gene Detection}

\timing{$\sim$10~min}

\begin{enumerate}

\item \textbf{Run the SVG detection script.}
\begin{lstlisting}
PYTHONPATH=src .venv/bin/python src/04_spatially_variable_genes.py
\end{lstlisting}

\item \textbf{What the script does:}
\begin{enumerate}
    \item Rebuilds the spatial neighbour graph.
    \item Computes Moran's $I$ for all genes using
          \texttt{sq.gr.spatial\_autocorr(mode="moran", n\_jobs=4)}.
    \item Saves ranked results to \texttt{results/04\_svg\_results.csv}.
    \item Plots the top 12 SVGs as spatial expression maps
          (\texttt{results/04\_top\_svgs.png}).
\end{enumerate}

\item \textbf{Inspect the CSV to explore SVG rankings.}
\begin{lstlisting}[style=python]
import pandas as pd
df = pd.read_csv("results/04_svg_results.csv")
print(df.head(20))          # top 20 SVGs
print(df[df["gene"] == "ERBB2"])   # check a gene of interest
\end{lstlisting}

\notepar{Genes with constant expression across spots (e.g.\ housekeeping
genes) produce \texttt{NaN} Moran's~$I$ and are excluded from ranking.
The console reports the number of such genes.}

\end{enumerate}

% -------------------------------------------------------------------
\subsection{Stage 5 — Neighbourhood Enrichment Analysis}

\timing{$\sim$5~min}

\begin{enumerate}

\item \textbf{Run the neighbourhood enrichment script.}
\begin{lstlisting}
PYTHONPATH=src .venv/bin/python src/05_neighbourhood_enrichment.py
\end{lstlisting}

\item \textbf{What the script does:}
\begin{enumerate}
    \item Assigns each spot its \emph{dominant cell type}: the cell type
          with the highest posterior mean abundance from Stage~3.
    \item Runs \texttt{sq.gr.nhood\_enrichment} with 1,000 permutations
          to compute co-localisation $z$-scores for every cell-type pair.
    \item Saves a two-panel figure: enrichment heatmap + spatial scatter
          (\texttt{results/05\_nhood\_enrichment.png}).
    \item Writes \texttt{results/05\_nhood\_enrichment.h5ad} for use in
          Stage~6.
\end{enumerate}

\item \textbf{Interpret the heatmap.}
Positive $z$-scores (red) indicate cell-type pairs that co-localise more
than expected by chance; negative values (blue) indicate mutual exclusion.
Strong Cancer Epithelial~--~CAF co-localisation ($z > 2$) is
characteristic of invasive ductal carcinoma.

\critical{The dominant cell-type assignment is a simplification: spots
contain mixtures of cell types.  For quantitative co-localisation,
consider using continuous abundance values directly.}

\end{enumerate}

% -------------------------------------------------------------------
\subsection{Stage 6 — GNN Tissue Domain Prediction}

\timing{$\sim$10~min (GPU)}

\begin{enumerate}

\item \textbf{Run the GNN prediction script.}
\begin{lstlisting}
PYTHONPATH=src .venv/bin/python src/06_gnn_domain_prediction.py
\end{lstlisting}

\item \textbf{What the script does:}
\begin{enumerate}
    \item Normalises, log-transforms, and computes PCA (50 components)
          on the raw counts.
    \item Builds a PyTorch Geometric \texttt{Data} object: nodes are
          spots (features = PCA embedding), edges from the spatial
          neighbour graph.
    \item Encodes dominant cell types as integer class labels.
    \item Splits spots into train/val/test (68/12/20\%).
    \item Trains a two-layer GCN for 300 epochs with Adam
          ($\text{lr}=10^{-3}$, weight decay $5\times10^{-4}$,
          dropout $p=0.3$).
    \item Saves training curves (\texttt{06\_gnn\_training.png}),
          a classification report (\texttt{06\_gnn\_metrics.txt}),
          and a side-by-side spatial prediction figure
          (\texttt{06\_gnn\_spatial\_pred.png}).
\end{enumerate}

\item \textbf{Read the classification report.}
\begin{lstlisting}
cat results/06_gnn_metrics.txt
\end{lstlisting}
Expected accuracy: 75--90\% depending on tissue composition.
Cancer Epithelial spots (majority class) typically reach F1~$>$~0.88.

\notepar{Rare cell types (Endothelial, Normal Epithelial) may show
zero recall due to class imbalance.  Consider weighted sampling or
oversampling these classes for more balanced evaluation.}

\item \textbf{Run on the second dataset for cross-dataset validation.}
\begin{lstlisting}
./scripts/run_pipeline.sh data/visium2 results2
\end{lstlisting}
Compare \texttt{results/06\_gnn\_metrics.txt} with
\texttt{results2/06\_gnn\_metrics.txt} to assess generalisability.

\pausept{All six result files in \texttt{results/} and
\texttt{results2/} represent the complete pipeline output.
Figures can be incorporated directly into a manuscript.}

\end{enumerate}

% ---------------------------------------------------------------
\section{Results}

\subsection{Quality control}

After applying QC filters (total counts $>1{,}000$, detected genes $>500$, mitochondrial fraction $<20\%$), \textbf{2,509 spots} were retained from the original Visium capture area. Figure~\ref{fig:qc} shows the distributions of total counts, number of detected genes, and mitochondrial gene fraction across all spots prior to filtering.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{../results/01_qc_distributions.png}
    \caption{QC metric distributions across Visium spots. From left to right: total UMI counts, number of detected genes, and percentage of reads mapping to mitochondrial genes. Dashed lines indicate filtering thresholds.}
    \label{fig:qc}
\end{figure}

\subsection{Clustering}

Leiden clustering at resolution 0.5 partitioned the 2,509 spots into spatially coherent clusters (Figure~\ref{fig:clusters}). The UMAP embedding reveals distinct transcriptional states, which overlay onto spatially contiguous regions of the tissue section, reflecting the organised architecture of breast cancer tissue.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{../results/02_clusters.png}
    \caption{Left: UMAP embedding of Visium spots coloured by Leiden cluster. Right: spatial scatter plot of the same clusters overlaid on the H\&E tissue image.}
    \label{fig:clusters}
\end{figure}

\subsection{Cell-type deconvolution}

Cell2location was applied to estimate the abundance of each histological cell-type category at each spot. The reference model was trained on the Wu et al. \citeyearpar{wu2021} atlas, yielding cell-type signatures for 18 distinct tissue region categories. Spatial maps of estimated cell-type abundances reveal the expected organisation of tumour, stromal, and immune compartments across the tissue section (Figure~\ref{fig:deconv}).

\begin{figure}[H]
    \centering
    \maybeincludegraphics{../results/03_cell_type_maps.png}
    \caption{Spatial maps of estimated cell-type abundances from Cell2location. Each panel shows the posterior mean abundance ($w_{sf}$) of one cell-type category, with warmer colours indicating higher abundance.}
    \label{fig:deconv}
\end{figure}

\subsection{Spatially variable genes}

Moran's $I$ was computed for all expressed genes. The top spatially variable genes—those whose expression is most strongly co-localised within the tissue—are shown in Figure~\ref{fig:svg}. High-ranking SVGs tend to mark specific tissue compartments such as the tumour core, stroma, or immune infiltrates.

\begin{figure}[H]
    \centering
    \maybeincludegraphics{../results/04_top_svgs.png}
    \caption{Spatial expression maps of the top 12 spatially variable genes ranked by Moran's $I$. Gene name and Moran's $I$ score are shown above each panel.}
    \label{fig:svg}
\end{figure}

\subsection{Neighbourhood enrichment}

Cell-type co-localisation was quantified by computing enrichment $z$-scores for all pairs of dominant cell-type categories. Figure~\ref{fig:nhood} shows the enrichment heatmap and the spatial map of dominant cell types per spot.

\begin{figure}[H]
    \centering
    \maybeincludegraphics{../results/05_nhood_enrichment.png}
    \caption{Left: neighbourhood enrichment $z$-score heatmap. Positive values (red) indicate cell-type pairs that are more co-localised than expected by chance; negative values (blue) indicate mutual exclusion. Right: spatial scatter plot coloured by the dominant cell type per spot.}
    \label{fig:nhood}
\end{figure}

\subsection{GNN tissue domain prediction}

The GCN was trained to predict the dominant cell-type label from the spatial gene expression graph. Classification performance on the held-out test set is reported in Table~\ref{tab:gnn}. The spatial domain predictions are compared to the ground-truth dominant cell type assignments in Figure~\ref{fig:gnn}.

\begin{table}[H]
    \centering
    \caption{GCN classification report on held-out test spots (20\% of all spots). Macro-averaged F1 accounts for class imbalance.}
    \label{tab:gnn}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Class} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-score} & \textbf{Support} \\
        \midrule
        B-cells            & 0.61 & 0.44 & 0.51 &  25 \\
        CAFs               & 0.70 & 0.68 & 0.69 &  91 \\
        Cancer Epithelial  & 0.88 & 0.92 & 0.90 & 190 \\
        Endothelial        & 0.00 & 0.00 & 0.00 &   6 \\
        Myeloid            & 0.74 & 0.81 & 0.77 &  72 \\
        Normal Epithelial  & 0.00 & 0.00 & 0.00 &   3 \\
        PVL                & 0.67 & 0.20 & 0.31 &  10 \\
        Plasmablasts       & 0.55 & 0.40 & 0.46 &  15 \\
        T-cells            & 0.65 & 0.73 & 0.69 &  90 \\
        \midrule
        Accuracy           & \multicolumn{3}{c}{0.75} & 502 \\
        Macro avg          & 0.53 & 0.46 & 0.48 & 502 \\
        Weighted avg       & 0.74 & 0.75 & 0.74 & 502 \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \maybeincludegraphics{../results/06_gnn_spatial_pred.png}
    \caption{Spatial comparison of ground-truth dominant cell type (left) and GCN predictions (right). Agreement between the two maps demonstrates the model's ability to learn tissue domain structure from gene expression and spatial adjacency alone.}
    \label{fig:gnn}
\end{figure}

% ---------------------------------------------------------------
\section{Troubleshooting}

Table~\ref{tab:trouble} lists problems users commonly encounter,
their likely causes, and recommended solutions.
Step numbers refer to the numbered steps in
Section~5 (Step-by-Step Method Details).

\begingroup
\small
\setlength{\LTpre}{6pt}
\setlength{\LTpost}{6pt}
\begin{longtable}{p{1.6cm} p{3.8cm} p{3.8cm} p{5.4cm}}
\caption{Troubleshooting table.}
\label{tab:trouble} \\
\toprule
\textbf{Step} & \textbf{Problem} & \textbf{Possible cause} & \textbf{Solution} \\
\midrule
\endfirsthead
\multicolumn{4}{c}{\small\itshape Table~\ref{tab:trouble} continued} \\
\toprule
\textbf{Step} & \textbf{Problem} & \textbf{Possible cause} & \textbf{Solution} \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\small\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

%% ── Before You Begin ────────────────────────────────────────────
Before you begin, step~3 &
\texttt{uv sync} fails with \textit{``Python~3.11 not found''} &
System Python is older than 3.11 &
Install Python~3.11 or 3.12 via \texttt{pyenv} or your system package
manager, then re-run \texttt{uv sync}. \\[4pt]

Before you begin, step~3 &
\texttt{ImportError: torch} after \texttt{uv sync} &
PyTorch was installed without CUDA support &
Install the CUDA-enabled wheel explicitly:
\texttt{uv pip install torch --index-url https://download.pytorch.org/whl/cu121}
then \texttt{uv sync --frozen}. \\[4pt]

Before you begin, step~3 &
\texttt{nvidia-smi} reports \textit{``No devices found''} &
NVIDIA kernel module not loaded &
Run \texttt{sudo modprobe nvidia}; if the module is missing, reinstall
the driver matching the running kernel with
\texttt{sudo apt install nvidia-dkms-\$VERSION}. \\[4pt]

Before you begin, step~5 &
Atlas file missing \texttt{adata.raw} (accessing \texttt{.raw} returns
\texttt{None}) &
CellXGene normalised version downloaded instead of raw counts &
Re-download the \textit{unprocessed} H5AD from CellXGene, which
includes the \texttt{raw} slot. Confirm with
\texttt{print(adata.raw)}. \\[4pt]

%% ── Stage 1 ─────────────────────────────────────────────────────
Stage~1, step~2c &
Fewer than 500 spots remain after QC filtering &
Tissue has low RNA quality (common with FFPE) &
Relax thresholds in \texttt{run\_qc()}: e.g.\
\texttt{total\_counts > 500} and \texttt{pct\_counts\_mt < 30}.
Re-inspect the QC histogram to choose appropriate cut-offs. \\[4pt]

Stage~1, step~2a &
\texttt{gzip.BadGzipFile} when reading SpaceRanger files &
Files have a \texttt{.gz} extension but are not gzip-compressed &
Check with \texttt{file barcodes.tsv.gz}. If plain text, strip the
extension: \texttt{mv barcodes.tsv.gz barcodes.tsv}, and repeat for
\texttt{features.tsv.gz} and \texttt{matrix.mtx.gz}. \\[4pt]

Stage~1, step~2e &
\texttt{UserWarning: flavor='seurat\_v3' expects raw count data} &
Normalisation applied before HVG selection &
The warning is harmless if QC was run before normalisation; HVGs are
still selected correctly. To suppress, move
\texttt{sc.pp.highly\_variable\_genes} before
\texttt{sc.pp.normalize\_total}. \\[4pt]

%% ── Stage 2 ─────────────────────────────────────────────────────
Stage~2, step~1 &
\texttt{ModuleNotFoundError: leidenalg} &
Package missing from environment &
Run \texttt{uv add leidenalg igraph \&\& uv sync} and restart. \\[4pt]

Stage~2, step~1 &
Leiden returns only one cluster &
Resolution parameter too low &
Increase \texttt{resolution} in \texttt{cluster()} to 0.8--1.5.
Alternatively, increase \texttt{n\_neighbors} in
\texttt{sc.pp.neighbors}. \\[4pt]

Stage~2, step~1 &
\texttt{coord\_type} argument raises \texttt{ValueError} &
Old Squidpy version removed the \texttt{"visium"} option &
Use \texttt{coord\_type="grid"} (current default for Visium). \\[4pt]

%% ── Stage 3 ─────────────────────────────────────────────────────
Stage~3, step~2e &
\texttt{GammaPoisson} likelihood produces \texttt{NaN} or loss explodes &
Normalised (non-integer) counts passed to Cell2location &
The script reloads raw Visium data from \texttt{data/visium/}.
Verify with \texttt{print(adata.X.data[:10])} — values must be
non-negative integers. \\[4pt]

Stage~3, step~2e &
\texttt{KeyError: 'means\_per\_cluster\_mu\_fg'} &
Cell2location version mismatch: old versions stored signatures in
\texttt{uns}, newer in \texttt{varm} &
Use \texttt{adata\_ref.varm["means\_per\_cluster\_mu\_fg"]}.
Confirm which slot is populated with
\texttt{print(adata\_ref.varm.keys())}. \\[4pt]

Stage~3, step~2d &
Shared gene count below 5,000 &
Gene identifier mismatch: atlas uses Ensembl IDs, Visium uses
HGNC symbols &
Access raw counts via \texttt{adata\_ref\_full.raw.to\_adata()} and
assign \texttt{adata\_ref.var\_names = adata\_ref.var["feature\_name"].values}
before intersecting. \\[4pt]

Stage~3, step~2e &
\texttt{RuntimeError: CUDA out of memory} &
Spatial model batch size too large &
Pass \texttt{batch\_size=256} to \texttt{model.train()} instead of
\texttt{batch\_size=None}. Training will be slower but memory-safe. \\[4pt]

Stage~3, step~2e &
\texttt{ValueError: shapes not aligned} or length mismatch &
Gene lists differ between Visium object and signature matrix &
Ensure the gene intersection step runs correctly:
\texttt{shared = adata\_raw.var\_names.intersection(signatures.index)}
and subset both objects before fitting the spatial model. \\[4pt]

%% ── Stage 4 ─────────────────────────────────────────────────────
Stage~4, step~2b &
All Moran's $I$ values are \texttt{NaN} &
Expression matrix contains no variation (all genes constant across
spots) &
This typically means the wrong \texttt{.h5ad} was loaded.
Confirm \texttt{adata.X} is not all zeros with
\texttt{print(adata.X.sum())}. \\[4pt]

Stage~4, step~2b &
Warning: \textit{``N variables were constant''} &
Some genes are expressed in only one spot &
Expected and harmless; those genes receive \texttt{NaN} and are
excluded from the ranking automatically. \\[4pt]

%% ── Stage 5 ─────────────────────────────────────────────────────
Stage~5, step~2a &
\texttt{TypeError: Expected adata.obs['dominant\_cell\_type'] to be
categorical} &
\texttt{idxmax} returns a \texttt{string}-dtype Series in newer Pandas &
Cast explicitly:
\texttt{adata.obs["dominant\_cell\_type"] = dominant.astype("category")}.
This is already applied in the current script. \\[4pt]

Stage~5, step~2b &
Neighbourhood enrichment $z$-scores are all near zero &
Too few spots assigned to minority cell types for permutation test
to be meaningful &
Increase permutations (\texttt{n\_perms=2000}) or merge rare cell
types into an ``Other'' category before running enrichment. \\[4pt]

Stage~5, step~2a &
Cell-type labels in the heatmap contain a long prefix
(e.g.\ \textit{meanscell\_abundance\_w\_sf\_\ldots}) &
Cell2location column names include a model-derived prefix &
The \texttt{assign\_dominant\_cell\_type} function strips the common
prefix automatically using \texttt{os.path.commonprefix}.
If labels are still long, print \texttt{abundances.columns} and
hard-code the prefix length. \\[4pt]

%% ── Stage 6 ─────────────────────────────────────────────────────
Stage~6, step~2a &
\texttt{KeyError: 'X\_pca'} &
PCA was not stored in the deconvolved \texttt{.h5ad} (Cell2location
works on raw counts without PCA) &
\texttt{compute\_pca(adata)} is called automatically at the start of
Stage~6; this is already implemented in the script. \\[4pt]

Stage~6, step~3 &
GNN test accuracy below 50\% &
Severe class imbalance: one cell type dominates (e.g.\ Cancer
Epithelial in tumour-dense tissue) &
Compute per-class weights and pass to the loss:
\texttt{weight = 1 / class\_counts; F.cross\_entropy(\ldots, weight=weight)}. \\[4pt]

Stage~6, step~3 &
Validation accuracy does not improve after epoch~50 &
Learning rate too high or model overfitting &
Reduce \texttt{LR} to \texttt{1e-4} in
\texttt{src/06\_gnn\_domain\_prediction.py}, or add an early-stopping
callback based on validation loss. \\[4pt]

Stage~6, step~4 &
Second dataset (\texttt{results2/}) produces very different accuracy
from dataset~1 &
Different tissue composition: one section may be tumour-dominated,
the other stroma-dominated &
This is biologically expected, not a pipeline error. Compare
weighted F1 (not accuracy) for a class-imbalance-robust metric. \\[4pt]

\end{longtable}
\endgroup

% ---------------------------------------------------------------
\section{Discussion}

This work presents a fully integrated pipeline for spatial transcriptomics analysis of breast cancer, combining established bioinformatics tools with graph-based deep learning. Several aspects merit discussion.

\paragraph{Data and deconvolution reference.} The deconvolution reference used here (Zenodo 4739739) contains histological region annotations rather than single-cell type labels. This provides a biologically coarser decomposition than would be achieved with a true single-cell atlas annotated at the cell-type level (e.g. distinguishing T cells from B cells and myeloid subsets). Future work should incorporate the full Wu et al. \citeyearpar{wu2021} single-cell atlas from GEO (GSE176078), which provides fine-grained cell-type annotations across $>26{,}000$ cells.

\paragraph{GCN for spatial domain prediction.} The GCN achieved 75\% test accuracy, with particularly strong performance on the dominant Cancer Epithelial class (F1 = 0.90). Rare classes such as Endothelial and Normal Epithelial had near-zero recall, reflecting severe class imbalance rather than model failure. Using dominant cell type as a proxy for tissue domain is a reasonable first approximation, but tissue domains in cancer are heterogeneous. Extensions could include multi-relational graphs (combining transcriptional and spatial edges), attention-based graph convolutions (GAT), or unsupervised graph clustering to discover domains without predefined labels.

\paragraph{Scalability.} With 2,509 spots and 18,714 genes the current dataset is modest; the pipeline is designed to scale to Visium HD ($\sim$11,000 spots at 8\,$\mu$m resolution) and larger cohorts with minor modifications.

% ---------------------------------------------------------------
\section{Conclusion}

We have demonstrated an end-to-end pipeline for spatial transcriptomics analysis of the breast cancer TME, integrating Bayesian cell-type deconvolution, spatial statistics, and graph neural networks. The pipeline is fully reproducible and publicly available. The GCN component in particular highlights how the spatial gene expression graph encodes sufficient information to predict tissue domain identity, opening avenues for spatially-aware deep learning in cancer genomics.

% ---------------------------------------------------------------
\section*{Data and code availability}

All analysis code is available at \url{https://github.com/Klas96/spatial-breast-cancer} under the MIT licence. The Visium dataset is freely available from 10x Genomics. The scRNA-seq reference is available from Zenodo (accession 4739739) and GEO (GSE176078).

% ---------------------------------------------------------------
\bibliographystyle{abbrvnat}
\bibliography{refs}

\end{document}
